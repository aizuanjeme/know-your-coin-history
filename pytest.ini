[pytest]
testpaths = tests
python_files = test_*.py
python_functions = test_*
addopts = -v --tb=short
markers =
    unit: Pure unit tests (no I/O)
    integration: Tests requiring mocked RPC
    api: Flask API endpoint tests
    slow: Tests that take longer to run
